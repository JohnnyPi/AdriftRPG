[package]
name = "terrain_generation"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
shared = { path = "../shared" }
voxel_core = { path = "../voxel_core" }

[dev-dependencies]
game_data = { path = "../game_data" }
