schema_version: 1
id: world.island_testbed

seed: 48129

voxel:
  cell_size_m: 1.0

chunks:
  cells: [16, 16, 16]
  # Centered: X/Z span [-256, +256), Y spans [-112, +112).
  # Authored op-based island showcase at 4× area (2× linear) vs the original testbed.
  world_extent: [32, 14, 32]

terrain: terrain.island_testbed
biomes: biomes.expanded_slice
materials: materials.expanded_slice
surface: surface.expanded_slice
water: water.tropical_shallow
lighting: lighting.late_morning
sky: sky.expanded_showcase

hydrology_bodies:
  - hydrology.upland_pool

ocean_extent_m: 576.0
coord_offset: [256.0, 0.0, 256.0]

resolution:
  regional_m: 8
  local_m: 4
  voxel_m: 1
